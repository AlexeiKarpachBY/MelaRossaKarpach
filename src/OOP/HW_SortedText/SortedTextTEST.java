package OOP.HW_SortedText;

import java.util.Arrays;

public class SortedTextTEST {

     /* String story = "Once upon a time a Wolf was lapping at a spring on a hillside, when, "
            .concat("looking up, what should he see but a Lamb just beginning to drink a little lower down.");

    SortedText text = new SortedText(story);
        text.removeAllCommasAndDots();
        System.out.println(text.getInfo());
        text.sortByCapitalLetter();
        System.out.println(text.getInfo());
        text.removeAllCommasAndDots();
        System.out.println(text.getInfo());
        text.getAlphabetSortedText();
        System.out.println(text.getInfo());

      *//*  String srt = text.getInfo();
        String[] test = srt.split("\\s[A-Z]"); // Регулярка неправильная
        for (String x : test) {
            System.out.println(x + "\n");
        }*/
     /*String[] arrayTest = text.getText().split("!");
        System.out.println(Arrays.toString(arrayTest));
    int count =0;


        for (String k: arrayTest) {
        if (k.length() > 3) count++;
    }

    String[] test = new String[count];
    int i = 0;
        for (String x : arrayTest) {
        if (x.length() > 3) {
            test[i] = x;
            i++;
        }*/

   /* String story = "Once upon a time a Wolf was lapping at a spring on a hillside, when, "
            .concat("looking up, what should he see but a Lamb just beginning to drink a little lower down.");

    SortedText text = new SortedText(story);
        text.removeAllCommasAndDots();
        text.sortByCapitalLetter();
        text.removeAllCommasAndDots();
        text.getAlphabetSortedText();

        for (String x : text.getArrayAllWordFromText()) {
        System.out.println(x);
    }*/
    }


